/**
 * Atoms Index - Exportación centralizada de componentes atómicos
 * 
 * Este archivo permite importar todos los componentes atómicos
 * desde un solo lugar para mantener los imports limpios.
 * 
 * @example
 * import { Button, Input, Badge, Card } from '@atoms';
 */

export { default as Button } from './Button';
export { default as Input } from './Input';
export { default as Badge } from './Badge';
export { default as Card } from './Card';
